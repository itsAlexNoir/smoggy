import os
from absl import flags, app
import logging





def main(argv):
    logging.info('_'*80)
    logging.info('Generate dataset!')
    logging.info('_' * 80 + '\n')









if __name__ == '__main__':
    FLAGS = flags.FLAGS
    #define_apps()
    app.run(main)